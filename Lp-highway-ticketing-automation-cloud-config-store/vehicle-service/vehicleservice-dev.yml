server:
  port: 9196
spring:
  application:
    name: vehicle
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/htavehicle
    username: root
    password: rootdts123
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: update
  profiles:
    active: dev