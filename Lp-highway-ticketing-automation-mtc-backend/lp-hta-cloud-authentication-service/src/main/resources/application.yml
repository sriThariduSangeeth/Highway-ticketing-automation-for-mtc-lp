server:
  port: 9098
spring:
  application:
    name: authservice
  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/htaauth
    username: root
    password: rootdts123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: update


jwt:
  secret: dtsangeeth

driver:
  service:
    base:
      uri:  http://localhost:9095